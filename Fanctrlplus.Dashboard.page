<!--
title: FanCtrl Plus
menu: Dashboard
icon: fanctrlplus.png
-->

<?php
$plugin = 'fanctrlplus';

// 加载状态（运行中 or 停止）
$status_map = @file_get_contents("/plugins/$plugin/include/FanctrlLogic.php?op=status_all");
$status_map = json_decode($status_map, true) ?? [];
?>

<table class="share_status small">
  <thead>
    <tr><th>Fan</th><th>Status</th></tr>
  </thead>
  <tbody>
    <?php foreach ($status_map as $name => $status): ?>
    <tr>
      <td><?=htmlspecialchars($name)?></td>
      <td><?=($status === 'running') ? '🟢 Running' : '🔴 Stopped'?></td>
    </tr>
    <?php endforeach; ?>
  </tbody>
</table>
